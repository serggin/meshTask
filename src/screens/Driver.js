import React from 'react';
import {Text, View} from 'react-native';
import globalStyles from '../styles';

const Driver = () => {
  return (
    <View style={globalStyles.screen}>
      <Text style={globalStyles.boxText}>Driver</Text>
    </View>
  );
};

export default Driver;
